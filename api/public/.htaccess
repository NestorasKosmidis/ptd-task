RewriteEngine On

# Αν υπάρχει πραγματικό αρχείο/φάκελος, σερβίρισέ το
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Αλλιώς, όλα στο index.php
RewriteRule ^ index.php [QSA,L]
